@startuml 05-example3
start
partition #lightGreen "ввод" {
    - Ввод длины массива len
}
partition #lightPink "управление памятью" {
    - Выделить память под массив a
}
partition #lightGreen "ввод" {
    while (i = 0; i < len; i++)
        - Ввод элемента массива a[i]
    endwhile
}
partition #lightGray "вычисления" {
    while (i = 0; i < len; i++)
        - res[i] = a[i] + b[i]
    endwhile
}
partition #lightGreen "вывод" {
    while (i = 0; i < len - 1; i++)
        if (a[i] > 0 и a[i + 1] > 0?) then
        else (да)
        - Вывод "a[i] и a[i + 1]"
        endif
    endwhile
}
partition #lightPink "управление памятью" {
    - Освободить память массива a
}
stop
@enduml
