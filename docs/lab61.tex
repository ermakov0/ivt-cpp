Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен номеру своей строки. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен номеру своего столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен сумме номера строки и номера столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен разности номера столбца и номера строки. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен произведению номера строки на номер столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: на чётных строках все элементы равны 1, на нечётных --- 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: в чётных столбцах все элементы равны 5, в нечётных --- 9. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: если сумма индексов строки и столбца чётная, элемент равен 1, иначе 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: если индекс строки кратен трём, элемент равен 7, иначе 2. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: если индекс столбца кратен трём, элемент равен 4, иначе 8. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен квадрату номера своей строки. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен квадрату номера своего столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен удвоенному номеру своей строки. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен утроенному номеру своего столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен остатку от деления номера строки на 2. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: все элементы первой строки равны 1, последней строки --- 2, остальные --- 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: все элементы первого столбца равны 3, последнего столбца --- 4, остальные --- 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: все элементы главной диагонали равны 1, остальные --- 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: все элементы побочной диагонали равны 5, остальные --- 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен остатку от деления номера столбца на 2. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен остатку от деления номера строки на 3. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен остатку от деления номера столбца на 3. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: элементы выше главной диагонали равны 1, ниже — 2, на диагонали — 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: элементы выше побочной диагонали равны 7, ниже — 8, на диагонали — 0. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: если строка чётная, элемент равен номеру столбца, иначе — номеру строки. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: если столбец чётный, элемент равен номеру строки, иначе — номеру столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен сумме квадратов номера строки и номера столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен модулю разности номера строки и номера столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен максимуму из номера строки и номера столбца. Вывести полученную матрицу в консоль.
Даны размеры матрицы $M \times N$. Заполнить матрицу по следующему правилу: каждый элемент равен минимуму из номера строки и номера столбца. Вывести полученную матрицу в консоль.
